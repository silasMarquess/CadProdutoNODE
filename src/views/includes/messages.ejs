<% if (errors) {%>
<div class="row">
    <div class="col my-3">
        <div class="aler alert-danger">
            <% errors.forEach(erro =>{ %>
                <%= erro %><br>
            <% }); %>
        </div>
    </div>
</div>
<%}%>